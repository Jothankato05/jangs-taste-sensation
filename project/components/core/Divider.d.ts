/** Hairline divider, optional centered caps label. */
export interface DividerProps {
  /** Centered wide-tracked caps label. */
  label?: string;
  /** Morning Fog tinted line. */
  accent?: boolean;
  style?: React.CSSProperties;
}
export declare function Divider(props: DividerProps): JSX.Element;
