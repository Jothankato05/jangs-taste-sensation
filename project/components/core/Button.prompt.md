Action button — uppercase, 0.22em tracking, sharp corners with a 16px top-right curve.

```jsx
<Button>Reserve a seat</Button>
<Button variant="outline" size="lg">View menu</Button>
<Button variant="accent" icon={<ArrowRight size={16} />}>Book now</Button>
```

Variants: primary (white), outline (hairline→fog on hover), ghost, accent (Morning Fog). Sizes sm/md/lg. Press scales 0.98.
