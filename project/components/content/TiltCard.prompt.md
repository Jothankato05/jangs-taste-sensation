3D cursor-tilt wrapper — the "3D cinematic" primitive. Wrap DishCards, feature panels, imagery.

```jsx
<TiltCard maxTilt={7}><img src="dish.jpg" style={{ width: "100%", display: "block" }} /></TiltCard>
```

Deep float shadow while hovered; slow cinematic settle on leave.
